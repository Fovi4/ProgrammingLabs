#include <iostream>
using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    if ( a + b > c and b + c > a and c + a > b )
        cout << "Можно создать треугольник";
    else
        cout << "Нельзя создать треугильник";
}
